@import '~styles/variables';

.container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 4rem;

  background: #FFFFFF;
  box-shadow: 0px 1px 3px 0px rgba(0,0,0,0.18);
  z-index: 1;

  div {
    min-width: 200px;
  }

  div:nth-child(2) {
    flex-grow: 1;
    text-align: center;
  }

  div:last-child {
    text-align: right;
  }
}

.avatarLink {
  padding: 0;

  img {
    border-radius: 3px;
  }
}
