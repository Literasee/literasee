.container {
  display: flex;
  flex-direction: column;
  flex-grow: 1;
  padding: 1.5rem;
}

.buttonContainer {
  display: flex;
  justify-content: flex-end;
  padding-top: 1rem;

  button:last-child {
    margin-left: 0.6rem;
  }
}
